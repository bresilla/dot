(defpoll workspace 
    :interval "0.1s"
    :initial "1"
    `hyprctl activeworkspace -j | jq '.id'`
)

(defpoll monitor 
    :interval "0.1s"
    :initial "1"
    `hyprctl activeworkspace -j | jq '.monitor'`
)


(defwindow workspaces_zero
    :monitor 0
    :stacking "fg"
    :windowtype "dock"
    ; :windowtype "normal"
    :wm-ignore true
    :geometry (geometry
        :x "50%"
        :y "0px"
        :width "100%"
        :height "4px"
        :anchor "top center")
    (box :class "wrkspc_box"
        (box :class "wrkspc_box")
        (box :class "wrkspc_box")
        (box :class "wrkspc_box" :spacing 10
            (box :class {workspace=="1" ? "wrkspc_active" : "wrkspc"})
            (box :class {workspace=="2" ? "wrkspc_active" : "wrkspc"})
            (box :class {workspace=="3" ? "wrkspc_active" : "wrkspc"})
            (box :class {workspace=="4" ? "wrkspc_active" : "wrkspc"})
            (box :class {workspace=="5" ? "wrkspc_active" : "wrkspc"})
            (box :class {workspace=="6" ? "wrkspc_active" : "wrkspc"})
        )
        (box :class "wrkspc_box")
        (box :class "wrkspc_box")
    )
)

(defwindow workspaces_one
    :monitor 1
    :stacking "fg"
    :windowtype "dock"
    ; :windowtype "normal"
    :wm-ignore true
    :geometry (geometry
        :x "50%"
        :y "0px"
        :width "100%"
        :height "4px"
        :anchor "top center")
    (box :class "wrkspc_box"
        (box :class "wrkspc_box")
        (box :class "wrkspc_box")
        (box :class "wrkspc_box" :spacing 10
            (box :class {workspace=="11" ? "wrkspc_active" : "wrkspc"})
            (box :class {workspace=="12" ? "wrkspc_active" : "wrkspc"})
            (box :class {workspace=="13" ? "wrkspc_active" : "wrkspc"})
            (box :class {workspace=="14" ? "wrkspc_active" : "wrkspc"})
            (box :class {workspace=="15" ? "wrkspc_active" : "wrkspc"})
            (box :class {workspace=="16" ? "wrkspc_active" : "wrkspc"})
        )
        (box :class "wrkspc_box")
        (box :class "wrkspc_box")
    )
)

(defwindow workspaces_two
    :monitor 2
    :stacking "fg"
    :windowtype "dock"
    ; :windowtype "normal"
    :wm-ignore true
    :geometry (geometry
        :x "50%"
        :y "0px"
        :width "100%"
        :height "4px"
        :anchor "top center")
    (box :class "wrkspc_box"
        (box :class "wrkspc_box")
        (box :class "wrkspc_box")
        (box :class "wrkspc_box" :spacing 10
            (box :class {workspace=="21" ? "wrkspc_active" : "wrkspc"})
            (box :class {workspace=="22" ? "wrkspc_active" : "wrkspc"})
            (box :class {workspace=="23" ? "wrkspc_active" : "wrkspc"})
            (box :class {workspace=="24" ? "wrkspc_active" : "wrkspc"})
            (box :class {workspace=="25" ? "wrkspc_active" : "wrkspc"})
            (box :class {workspace=="26" ? "wrkspc_active" : "wrkspc"})
        )
        (box :class "wrkspc_box")
        (box :class "wrkspc_box")
    )
)

(defwindow workspaces_three
    :monitor 3
    :stacking "fg"
    :windowtype "dock"
    ; :windowtype "normal"
    :wm-ignore true
    :geometry (geometry
        :x "50%"
        :y "0px"
        :width "100%"
        :height "4px"
        :anchor "top center")
    (box :class "wrkspc_box"
        (box :class "wrkspc_box")
        (box :class "wrkspc_box")
        (box :class "wrkspc_box" :spacing 10
            (box :class {workspace=="31" ? "wrkspc_active" : "wrkspc"})
            (box :class {workspace=="32" ? "wrkspc_active" : "wrkspc"})
            (box :class {workspace=="33" ? "wrkspc_active" : "wrkspc"})
            (box :class {workspace=="34" ? "wrkspc_active" : "wrkspc"})
            (box :class {workspace=="35" ? "wrkspc_active" : "wrkspc"})
            (box :class {workspace=="36" ? "wrkspc_active" : "wrkspc"})
        )
        (box :class "wrkspc_box")
        (box :class "wrkspc_box")
    )
)