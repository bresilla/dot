import QtQuick
import ".."

BaseCard {
    id: root
    
    Text {
        anchors.centerIn: parent
        text: "System"
        font.pixelSize: Theme.fontSizeLarge
        color: Theme.surfaceText
    }
}
