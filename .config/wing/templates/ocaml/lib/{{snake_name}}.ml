(* Mirrors the version in dune-project, which is the one place that holds it. *)
let version = "0.1.0"

(* The one thing this library does, so the starter has something to test. *)
let greeting () = "hello from {{kebab_name}}"
